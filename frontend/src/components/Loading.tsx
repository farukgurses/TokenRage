import React from "react";
import ReactLoading from "react-loading";

const Loading = (): JSX.Element => (
  <ReactLoading type="cylon" color="#70C2A2" height={62} width={62} />
);

export default Loading;
