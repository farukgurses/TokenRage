import React from "react";
import ReactLoading from "react-loading";

const Loading = (): JSX.Element => (
  <ReactLoading type="cylon" color="#fff" height={62} width={62} />
);

export default Loading;
